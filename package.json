{
  "name": "career",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ career",
    "create-local": "graph create --node http://localhost:8020/ career",
    "remove-local": "graph remove --node http://localhost:8020/ career",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 career",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.71.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
